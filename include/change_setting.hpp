#pragma once

int Change_brightness(bool top_screen, bool bottom_screen, int bridgeness);

int Wifi_disable();

int Wifi_enable();

int Change_screen_state(bool top_screen, bool bottom_screen, bool state);